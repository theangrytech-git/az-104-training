name: call-gitleaks

on:
  workflow_dispatch:

jobs:
  run-gitleaks:
    uses: theangrytech-git/MSDO/.github/workflows/gitleaks-reusable.yml@main
    with:
      branch: main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
